-#  This file is app/views/movies/find_movies_same_director.html.haml
%h1 Movies with same director

%table#movies
  %thead
    %tr
      /As for previous version of Ruby @selected_ratings was not converted to hash, but to a string.
      /%th{:class => @title_header}= link_to 'Movie Title', movies_path(:sort => 'title', :ratings => @selected_ratings), :id => 'title_header'
      /%th Rating
      /%th{:class => @date_header}= link_to 'Release Date', movies_path(:sort => 'release_date', :ratings => @selected_ratings), :id => 'release_date_header'
      %th= link_to 'Movie Title', controller: 'movies', action: 'index', id: 'title_header', sort_by: 'title'
      %th Rating
      %th= link_to 'Release Date', controller: 'movies', action: 'index', id: 'release_date_header', sort_by: 'release_date'
      %th More Info
  %tbody
    - @movies_same_director.each do |movie|
      %tr
        %td= movie.title 
        %td= movie.rating
        %td= movie.release_date
        %td= link_to "More about #{movie.title}", movie_path(movie)

= link_to 'Back to movie list', movies_path
